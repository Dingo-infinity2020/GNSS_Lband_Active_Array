# PROJECT_HANDOFF.md

> Canonical operational handoff file for design side and execution host H01.

## MACHINE-READABLE HEADER

```text
HANDOFF_VERSION=4
CANONICAL_BRANCH=project/r0-charts-scaffold
CURRENT_GATE=R1-CHARTS-GNSS-DERIVATIVE
CURRENT_TASK_ID=R1A-DESIGN-BASELINE-DEFINITION
TASK_OWNER=DESIGN
TASK_STATUS=READY_FOR_DESIGN
SOLVER_PERMISSION=NO
OPTIMIZATION_PERMISSION=NO
L_BAND_SCALING_PERMISSION=DESIGN_ONLY
LNA_INTEGRATION_PERMISSION=NO
HARDWARE_PERMISSION=NO
```

## 1. Sync protocol

Execution host shortcut:

```powershell
.\scripts\sync_handoff.ps1
```

Before work, read:
1. `docs/PROJECT_RULES.md`
2. `docs/DECISIONS.md`
3. `AGENTS.md`
4. this file

Only the side named by `TASK_OWNER` may advance the task.

## 2. Mainline status

MAINLINE:
- CHARTS-inspired planar balanced active element.

FIRST_BACKUP:
- PUMA / unbalanced tightly-coupled element.

R0 CHARTS is closed as:

`R0_CLOSED_SOURCE_LIMITED`

See:
- `docs/R0_CHARTS_CLOSURE_SOURCE_LIMITED.md`

Accepted from R0:
- visible V0.3 CHARTS aperture topology,
- 200 mm source-scale ground separation,
- center-active integration philosophy,
- balanced/differential active-feed concept.

Not available from public CHARTS source:
- exact hidden center-feed geometry,
- exact local-feed ground,
- exact substrate stack.

These missing details will become explicit PROJECT_DESIGN choices in R1.

## 3. REF-CUI status

REF-CUI remains **REFERENCE_ONLY**.

H01 R0B:
- CST execution/replay PASS,
- scientific geometry fidelity HOLD after design-side comparison to the user-supplied publisher PDF.

See:
- `evidence/ref_cui_r0b_h01_20260923_1341/`
- `evidence/REF_CUI_R0B_DESIGN_REVIEW_SCIENTIFIC_HOLD.md`
- Decision D0008.

No REF-CUI solver is authorized.

If REF-CUI is ever rebuilt, one canonical arm+slot must be constructed and all other arms generated by exact 90-degree rotations.

## 4. Current design-side task

Task:

**R1A-DESIGN-BASELINE-DEFINITION**

Read:
- `docs/R1_CHARTS_GNSS_DERIVATIVE_PLAN.md`
- accepted CHARTS V0.3 provenance/evidence.

Design side must prepare:
- source-to-R1 scale manifest,
- project-owned center-feed geometry specification,
- exact X/Y symmetry rule,
- R1A BUILD-ONLY acceptance criteria.

Initial geometric scale factor:
- 0.2857142857 (400 MHz -> 1.40 GHz nominal center).

This is a baseline definition only.

No solver.
No optimization.
No LNA.

## 5. HOST TASK

**NONE.**

H01 should sync/read and wait.

Do not continue REF-CUI work.
Do not start R1 CST until a later handoff explicitly returns the baton to H01.

## 6. Most recent host result

```text
HOST=H01
TASK=REF-CUI-R0B-BUILD-ONLY-H01
HOST_REPORTED_STATUS=PASS_REF_CUI_R0B_BUILD_ONLY
DESIGN_REVIEW_STATUS=HOLD_REF_CUI_R0B_SCIENTIFIC_GEOMETRY_MISMATCH
EVIDENCE=evidence/ref_cui_r0b_h01_20260923_1341/
SOLVER_RUN=NO
```

## 7. Stop rule

The next host task will only be issued after the R1A project-owned geometry/feed/symmetry specification is frozen.

No solver, optimization, LNA integration, or hardware release is authorized.
